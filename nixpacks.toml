[phases.setup]
nixPkgs = ['python311', 'openssl']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Building with templates and static files..."']

[start]
cmd = 'python app.py'
